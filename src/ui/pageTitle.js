export function pageTitle(text) {
    return `
        <div class="container">
            <div class="row">
                <h1 class="col-12 display-1 mb-5">${text}</h1>
            </div>
        </div>`;
}